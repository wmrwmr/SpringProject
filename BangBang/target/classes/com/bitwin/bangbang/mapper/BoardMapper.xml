<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bitwin.bangbang.mapper.notice_boardMapper">

	<select id="getList" resultType="com.bitwin.bangbang.domain.notice_boardVO">
	
	<![CDATA[ 
	select * from tbl_notice_board where nidx > 0
	]]>

	</select>
	
	<insert id="insert">
		insert into tbl_notice_board (nidx,title,content)
		values (seq_notice_board.nextval, #{title}, #{content})
	</insert>
	
	<insert id="insertSelectKey">
		<selectKey keyProperty="nidx" order="BEFORE" 
			resultType="long">
			select seq_notice_board.nextval from dual
		</selectKey>
		
		insert into tbl_notice_board (nidx,title,content)
		values (#{nidx}, #{title}, #{content})
	</insert>
	
	
	<select id="read" resultType="org.zerock.domain.notice_boardVO">
		select * from tbl_notice_board where nidx = #{nidx}
	</select>
	
	<delete id="delete">
		delete from tbl_notice_board where nidx = #{nidx}
	</delete>
	
	<update id="update">
		update tbl_notice_board
		set title = #{title}, 
		content = #{content},
		updateDate = sysdate
		where nidx = #{nidx}
	</update>
	
	
	<select id="getListWithPaging" resultType="org.zerock.domain.notice_boardVO">
	
		<![CDATA[
			select nidx, title, content, regdate, updatedate from (
				select /*+ INDEX_DESC(tbl_notice_board pk_notice_board) */ rownum rn, nidx, title, content, regdate, updatedate from tbl_notice_board where rownum <= #{pageNum} * #{amount}
			
			) where rn > (#{pageNum} -1) * #{amount}
		
		]]>

	</select>
	
	
	
	
	
	
	
	
	


</mapper>